/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { Fdfd } from "./Fdfd";
import { FdfdCountArgs } from "./FdfdCountArgs";
import { FdfdFindManyArgs } from "./FdfdFindManyArgs";
import { FdfdFindUniqueArgs } from "./FdfdFindUniqueArgs";
import { DeleteFdfdArgs } from "./DeleteFdfdArgs";
import { FdfdService } from "../fdfd.service";
@graphql.Resolver(() => Fdfd)
export class FdfdResolverBase {
  constructor(protected readonly service: FdfdService) {}

  async _fdfdsMeta(
    @graphql.Args() args: FdfdCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [Fdfd])
  async fdfds(@graphql.Args() args: FdfdFindManyArgs): Promise<Fdfd[]> {
    return this.service.fdfds(args);
  }

  @graphql.Query(() => Fdfd, { nullable: true })
  async fdfd(@graphql.Args() args: FdfdFindUniqueArgs): Promise<Fdfd | null> {
    const result = await this.service.fdfd(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => Fdfd)
  async deleteFdfd(@graphql.Args() args: DeleteFdfdArgs): Promise<Fdfd | null> {
    try {
      return await this.service.deleteFdfd(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
